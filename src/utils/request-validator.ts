import { ValidationError } from './error-handler';\n\nexport interface ValidationSchema {\n  [key: string]: {\n    required?: boolean;\n    type?: 'string' | 'number' | 'boolean' | 'object' | 'array';\n    minLength?: number;\n    maxLength?: number;\n    min?: number;\n    max?: number;\n    pattern?: RegExp;\n    enum?: unknown[];\n    custom?: (value: unknown) => boolean;\n  };\n}\n\nexport class RequestValidator {\n  static validate(data: Record<string, unknown>, schema: ValidationSchema): Record<string, unknown> {\n    const validated: Record<string, unknown> = {};\n    const errors: string[] = [];\n\n    for (const [key, rules] of Object.entries(schema)) {\n      const value = data[key];\n\n      // Check required\n      if (rules.required && (value === undefined || value === null || value === '')) {\n        errors.push(`${key} is required`);\n        continue;\n      }\n\n      // Skip validation if not required and not provided\n      if (!rules.required && (value === undefined || value === null)) {\n        continue;\n      }\n\n      // Type validation\n      if (rules.type) {\n        const actualType = Array.isArray(value) ? 'array' : typeof value;\n        if (actualType !== rules.type) {\n          errors.push(`${key} must be of type ${rules.type}, got ${actualType}`);\n          continue;\n        }\n      }\n\n      // String validations\n      if (typeof value === 'string') {\n        if (rules.minLength && value.length < rules.minLength) {\n          errors.push(`${key} must have at least ${rules.minLength} characters`);\n          continue;\n        }\n        if (rules.maxLength && value.length > rules.maxLength) {\n          errors.push(`${key} must have at most ${rules.maxLength} characters`);\n          continue;\n        }\n        if (rules.pattern && !rules.pattern.test(value)) {\n          errors.push(`${key} does not match required pattern`);\n          continue;\n        }\n      }\n\n      // Number validations\n      if (typeof value === 'number') {\n        if (rules.min !== undefined && value < rules.min) {\n          errors.push(`${key} must be at least ${rules.min}`);\n          continue;\n        }\n        if (rules.max !== undefined && value > rules.max) {\n          errors.push(`${key} must be at most ${rules.max}`);\n          continue;\n        }\n      }\n\n      // Enum validation\n      if (rules.enum && !rules.enum.includes(value)) {\n        errors.push(`${key} must be one of: ${rules.enum.join(', ')}`);\n        continue;\n      }\n\n      // Custom validation\n      if (rules.custom && !rules.custom(value)) {\n        errors.push(`${key} failed custom validation`);\n        continue;\n      }\n\n      validated[key] = value;\n    }\n\n    if (errors.length > 0) {\n      throw new ValidationError('Validation failed', { errors });\n    }\n\n    return validated;\n  }\n\n  static validateTask(data: unknown): Record<string, unknown> {\n    if (typeof data !== 'object' || data === null) {\n      throw new ValidationError('Task must be an object');\n    }\n\n    const schema: ValidationSchema = {\n      goal: {\n        required: true,\n        type: 'string',\n        minLength: 1,\n        maxLength: 500\n      },\n      data: {\n        required: true,\n        type: 'object'\n      },\n      priority: {\n        required: false,\n        type: 'string',\n        enum: ['low', 'medium', 'high']\n      }\n    };\n\n    return this.validate(data as Record<string, unknown>, schema);\n  }\n}\n"
  