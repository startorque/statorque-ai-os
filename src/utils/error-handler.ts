import logger from './logger';\n\nexport class AppError extends Error {\n  constructor(\n    public statusCode: number,\n    public message: string,\n    public context?: Record<string, unknown>\n  ) {\n    super(message);\n    this.name = 'AppError';\n    Error.captureStackTrace(this, this.constructor);\n  }\n}\n\nexport class ValidationError extends AppError {\n  constructor(message: string, context?: Record<string, unknown>) {\n    super(400, message, context);\n    this.name = 'ValidationError';\n  }\n}\n\nexport class NotFoundError extends AppError {\n  constructor(message: string, context?: Record<string, unknown>) {\n    super(404, message, context);\n    this.name = 'NotFoundError';\n  }\n}\n\nexport class UnauthorizedError extends AppError {\n  constructor(message: string, context?: Record<string, unknown>) {\n    super(401, message, context);\n    this.name = 'UnauthorizedError';\n  }\n}\n\nexport class InternalServerError extends AppError {\n  constructor(message: string, context?: Record<string, unknown>) {\n    super(500, message, context);\n    this.name = 'InternalServerError';\n  }\n}\n\nexport interface ErrorResponse {\n  status: number;\n  message: string;\n  timestamp: string;\n  context?: Record<string, unknown>;\n}\n\nexport const formatError = (error: Error | AppError): ErrorResponse => {\n  const timestamp = new Date().toISOString();\n\n  if (error instanceof AppError) {\n    logger.error(`[${error.name}] ${error.message}`, error.context);\n    return {\n      status: error.statusCode,\n      message: error.message,\n      timestamp,\n      context: process.env.NODE_ENV === 'development' ? error.context : undefined\n    };\n  }\n\n  logger.error(`[UnknownError] ${error.message}`, { stack: error.stack });\n  return {\n    status: 500,\n    message: process.env.NODE_ENV === 'production' ? 'Internal Server Error' : error.message,\n    timestamp\n  };\n};\n\nexport const handlePromiseRejection = (reason: unknown): void => {\n  if (reason instanceof Error) {\n    logger.error('[UnhandledRejection]', { message: reason.message, stack: reason.stack });\n  } else {\n    logger.error('[UnhandledRejection]', reason);\n  }\n};\n"
  